# 開発者向けメモ

利用者向けの説明は [README.md](README.md) にあります。ここには、実装をそう決めた理由と、
ビルド・テスト・リリースの手順を残します。

## 構成

```
src/common.h     共通定義（設定構造体・ボタン/組み合わせの定義）
src/main.c       常駐本体。トレイ、フック設置、前面ウィンドウ監視
src/chord.c      同時押し判定の中核。状態機械とイベント抹殺・合成注入
src/config.c     mayous.ini の読み書きとキーコンボ解析
src/agent.c      ホイール注入専用の子プロセス
src/settings.c   設定ウィンドウ（コントロールを直接生成）
src/capture.c    キー入力の記録
src/theme.c      システムのライト/ダークに合わせた配色
src/legacy.c     昔のバージョンが残したレジストリ登録の掃除
res/             アイコン、マニフェスト、バージョン情報
tools/           アイコン生成、テストハーネス
                 probe.c   … フック鎖に入って観測する
                 target.c  … 本物のウィンドウとしてアプリ視点を記録する
                 regkey.c  … 登録キーの発火を mayous の下流から観測する
                 uilib.ps1 … 他プロセスの GUI を安全に叩く共通部品
```

C + Win32 API のみ。依存は Windows 標準の DLL だけで、`-static` リンクにより
配布先に libgcc 等は不要です。

低レベルフックは**それを設置したスレッドのメッセージループ上で呼ばれます**。
つまりフック処理・タイマー・トレイ操作・設定ウィンドウはすべて同一スレッドで
直列化されるため、ロックを一切持たずに状態を触れます。この性質に全面的に
寄りかかった設計になっています。

---

## 仕組み

同時押しの本質的な難所は「先に押されたボタンのクリックをどう殺すか」です。
押下の瞬間には、それが修飾キー役なのか通常のクリックなのか判別できません。
そこで **押下を飲み込んで保留し、その後に起きたことで運命を決めます**。

```
 PS_IDLE ──押下(フックで握り潰す)──> PS_PENDING
                                        │
        相方ボタン / ホイールが来た ────┼──> PS_CONSUMED
                                        │      同時押し成立。離上も殺す
        しきい値を超えて移動した ───────┼──> PS_PASSTHRU
        長押しタイムアウト ─────────────┘      本物の押下を合成注入して以降素通し
                                        │      (ドラッグ・長押しの救済)
        何も起きずに離された ───────────┴──> 単独クリックとして成立させる
                                               (別機能への差し替えも可)
```

この結果、通常の右クリックは**押した瞬間ではなく離した瞬間**に発火します。
素早いクリックなら体感できませんが、原理上そうなります。

**この代償は左ボタンでは許容できません。** ウィンドウの切り替えもドラッグの起点も
左クリックなので、0.2 秒の遅れがそのまま「反応が鈍い」「枠を掴み損ねる」として
出ます。かつては設定画面に「左クリック」タブを置き、注意書き付きで選べるように
していましたが、**v13 で取りやめました**（`PFX_CAN()` が BTN_L を弾きます）。
`g_armed[BTN_L]` は必ず FALSE になり、左のイベントはフックで即座に素通しします。
復活させるときに触るのは 2 箇所だけです — `common.h` の `PFX_CAN()` と、
`settings.c` の `kTabPfx[]`。ini の `LeftThen...` は消していないので、
戻せばそのまま効きます。

ドラッグ判定で保留を解くときは、**押した位置まで戻してから押下を注入し、
今の位置まで動かす**という 3 イベントを 1 回の SendInput で送ります
(`emit_drag_start`)。今のカーソル位置で押下すると、既にしきい値ぶん離れているため
ウィンドウの枠のような細い当たり判定を掴み損ねます。

プレフィクス（先に押す側）になれるのは 右・サイド1・サイド2 の 3 つ。
中ボタンは押しっぱなしにする用途が無く、オートスクロールと衝突するため
サフィックス専用にしてあります。組み合わせは `CH_ID(pfx, suf) = pfx * SUF_COUNT + suf`
で機械的に決まり、18 通り + 登録キーが有効です。

### 登録キー

サフィックスの並びの後ろ（`SUF_KEY0` 以降）に「登録キー」の枠を `REGKEY_COUNT` 個
足してあります。**サフィックスとして足したので、設定の読み書き・設定画面のループ・
同時押しの成立判定は、既存のコードがそのまま面倒を見ます。** 違うのは 2 点だけです。

- どのキーがトリガーかは利用者が決めるので、`Config.regKeyVk[pfx][idx]` に持つ
  （ini は `RightThenKey1Trigger=f13`）。プレフィクスごとに別々に持たせてあるので、
  「右 + F13」と「サイド1 + F13」を別の動作にできます。
- 入口がマウスフックではなくキーボードフック（`chord_on_key`）になる。

キーボードフックは**登録キーが実際に使われているときだけ**張ります
（`main.c` の `keyhook_sync()` / `needs_key_hook()`）。1 つも登録が無ければ
キーストロークの経路には一切現れません。

**割り当ての無いキーでプレフィクスを昇格させてはいけません。** ボタンが後から
押された場合は、割り当てが無ければ保留を解いて世に出しますが（物理状態に忠実に
するため）、キーボードで同じことをすると、右ボタンを押したまま何か文字を打った
だけで右クリックが確定し、メニューが飛び出します。一致したときだけ触り、
それ以外は見なかったことにして素通しします。

押下を握り潰したキーは、**離上も必ず握り潰します**（`g_keySwVk[]`）。片方だけ
世に出すと、相手のアプリにはそのキーが押しっぱなしのまま残ります。オートリピートで
同じ押下が繰り返し届くのも、この控えで弾いています（連打にしないため）。

自分が `SendInput` したイベントには専用のタグ（`dwExtraInfo = MAYOUS_TAG`）を付け、
フック側で無条件に素通しさせることで再入と無限ループを断ち切っています。

---

## 踏んだ罠と、その回避策

実測して初めて分かったもの、あるいは実際に不具合として現れたものだけを書いています。

### 1. フックの中で SendInput を呼んではいけない

低レベルフックのコールバックの中で `SendInput` を呼ぶと、**実測で 250〜450ms
戻ってきません**。注入した入力を処理するのは、今まさにこちらの応答を待っている
当のスレッドだからです。その間に Windows は `LowLevelHooksTimeout` でフックを見限り、
**握り潰したはずのイベントをアプリへ配送してしまいます**。

結果、押下だけが消えて離上が届く／クリックが二重になる、といった収拾のつかない
壊れ方をします。実測ログ:

```
137076375  evt 0x0201 (左押下)  -> SWALLOW
137076468  evt 0x0202 (左離上)  ハンドラ開始
137076687  APP  LEFT UP         <<< 生の離上がアプリに漏れた
137076921  inject CLICK         453ms 後にようやく完了。もう手遅れ
```

そこで**フックの中では「握り潰すか否か」の判断だけを行い、実際の注入は
キューに積んでメッセージループへ回しています**（`chord.c` の `q_push` / `chord_pump`）。
フックから戻った直後に処理されるので、実測でフック→アプリ到達まで約 16ms です。

`chord_reset()` は注入をキューに積むだけなので、**終了処理では必ず `chord_pump()`
を呼ぶこと**。忘れると、注入済みの押下を閉じられないままボタンが押しっぱなしになります。

### 2. GetAsyncKeyState は握り潰した押下を見ていない

押下をフックで握り潰すと、その押下は **OS のキー状態に一切記録されません**。
つまり保留中は `GetAsyncKeyState(VK_RBUTTON)` が、ユーザーが実際に押していても
「離されている」と答えます。

これを知らずに「2 秒経っても押されていない＝取りこぼした残骸」として状態を
破棄していたため、**右ボタンを 2 秒以上押していると同時押しが効かなくなり、
さらに離上だけがアプリに素通しして両ボタンの挙動が壊れる**という不具合になっていました。

現在は、自分で押下を注入した `PS_PASSTHRU` のときだけ `GetAsyncKeyState` を
信用し、保留中は判定に使っていません（押しっぱなしは正当な状態なので）。
取りこぼしの回復は次の 3 段構えです:

- **押下時の辻褄合わせ**（`reconcile`）… 前回が決着していないボタンに押下が来たら、
  そこで清算する。「押下だけがアプリに届いて離上は握り潰される」＝ボタンが
  押されっぱなし、という最悪の事故を構造的に防ぐ最後の砦。
- **デスクトップ切替の検出** … UAC のセキュアデスクトップやロック画面へ移ると、
  その間の離上は届かない。`EVENT_SYSTEM_DESKTOPSWITCH` で検出して状態を捨てる。
- **60 秒の保険** … それでも残った場合のみ畳む。

有効/無効の切り替えでは**状態を捨てません**。捨てると、押している最中に
切り替わった場合に「押下は握り潰したのに離上は素通し」という不整合が生まれます。
無効化は「これ以上新しく乗っ取らない」だけを意味します。

### 3. ホイール注入だけ子プロセスが要る

Windows は **`WH_MOUSE_LL` を保持しているプロセスからのホイール注入を黙って捨てます**。
`SendInput` は `1` を返し `GetLastError` も `0` なのに、イベントは自分のフックにすら
現れません。実測:

| 条件 | 結果 |
|---|---|
| フック保持 + メインスレッドから HWHEEL | 消滅 |
| フック保持 + 別スレッドから HWHEEL | 消滅（プロセス単位の制約） |
| フック保持 + 縦ホイール | 消滅（水平に限らない） |
| フックを外した直後に HWHEEL | 通る |

そこで **フックを一切持たない注入専用の子プロセス**を、同じ exe の `--wheel-agent`
モードとして起動しています（`agent.c`）。本物のチルトホイールと完全に同じ経路を
通るので、水平スクロールに対応したアプリなら等しく反応します。
子プロセスは親の終了を `MsgWaitForMultipleObjects` で待ち、道連れで終わります。

一時的にフックを外して注入する案は、その隙間に生のホイールが素通ししてしまい、
連続スクロール中に縦スクロールが漏れるため採用していません。

### 4. 入れ子のモーダルループで PostQuitMessage を呼ばない

キー記録ウィンドウ（`capture.c`）は自前のモーダルループを持ちます。その中の
`WM_DESTROY` で `PostQuitMessage` を呼ぶと、ループを抜けた後も `WM_QUIT` が
キューに残り、**本体のメッセージループがそれを拾って mayous ごと終了します**
（実際にそうなりました）。ループは `g_wnd` が NULL になったことで抜けます。

記録中に本体が終了要求を受けた場合だけは、`WM_QUIT` を投げ直して外側へ伝えます。

### 5. タブコントロールの iImage

`TCITEM` の `iImage` は `-1` にすること。`0` のままだとイメージリストを設定して
いなくても画像 0 を取りに行き、環境によっては選択タブの描き直しで落ちます。

### 6. 押した瞬間に離すキーは「見えない」ことがある

押下と離上を間髪入れずに送ると、キーボードフックを張っているアプリは拾えますが、
`GetAsyncKeyState` を一定間隔で見に行く方式のアプリは取りこぼします。
押されている時間がほぼ 0 なので、サンプリングの隙間に丸ごと収まってしまうためです。

実例: zoom-pon のキー登録が反応しない。zoom-pon は毎 tick で 0x08〜0xFE の 247 個の
VK を `GetAsyncKeyState` で走査しており、Python のオーバーヘッドで 1 tick が
公称の 8ms より遥かに長くなります。C で書いた 8ms のポーラーでは拾えたので、
「低レベル入力として出ていない」わけではありません（フックにも `LLKHF_INJECTED`
付きで現れます）。純粋に押下時間の問題でした。

**そこで、同時押しに割り当てたキーは「プレフィクスを離すまで押しっぱなし」を
既定の挙動にしました。** 相手の周期がどれだけ伸びていても、押しっぱなしなら
必ず観測されます。かつては `hold:` を付けた時だけの特別扱いでしたが、
特別扱いする理由が無かった、というのが結論です。

押しっぱなしを既定にできるのは、**注入した押下がオートリピートしない**からです。
タイプマティックはキーボード側が作るもので、`SendInput` の押下を Windows が
勝手に繰り返すことはありません（`tools\test_autorepeat.ps1` で実測: 実物の
エディットコントロールに 3 秒押しっぱなしにしても入るのは 1 文字）。
だから `Ctrl+W` を押しっぱなしにしてもタブが次々に閉じる事故は起きません。
`Alt+Tab` に至っては、押している間ウィンドウ一覧が出たままになり離すと確定する、
という本来の挙動になります。

離す基準は**プレフィクスの離上**です。サフィックス（後から押す側）は叩いてすぐ
離すのが普通なので、そちらを基準にすると結局一瞬になってしまいます。
ホイールをサフィックスにした場合はそもそも離上が存在しません。

押しっぱなしにできない 2 つの場合だけ、従来どおり順に叩きます。
`TIMER_KEYPLAY` で「押す → 離す → 次のステップ」と段階を進めます
（待つのに `Sleep` は使えません。メッセージループが止まり、フックの配送も止まる）。

- 複数ステップ（`Ctrl+C` のあと `Ctrl+V`）── 押しっぱなしにしようがない
- 単独クリック（サイドボタン）── 離した時点で発火するので押しようがない

`KeyHoldMs` は**上限ではなく下限**として働きます。同時押しが一瞬で終わっても
この時間までは離上を遅らせます（`TIMER_KEYREL_BASE + pfx`）。

> **踏んだ穴。** `KeyHoldMs` は既定 ini に書き出しておきながら、`cfg_load()` に
> 読み取る行が無く、`g_cfg.keyHoldMs` はずっと 0 のままでした。`hold_ms()` は
> `> 0 ? … : 1` と防御していたので誰もクラッシュせず、`SetTimer(…, 1)` が
> タイマー解像度どおり約 15ms で発火する ── つまり**設定値に関係なく常に約 15ms**
> しか押していませんでした。`tools\test_keydur.ps1` で実測すると
> `KeyHoldMs=40` でも `600` でも `11 15 7 18 15 14` (ms) と全く同じで、これが
> 決め手になりました。設定項目を足すときは「書き出し・読み取り・既定値・
> クランプ」の 4 点が揃っているかを必ず確認すること。ゼロを無害な値へ
> 丸めるガードは、未初期化をバグではなく仕様に見せかけてしまいます。

実測（実物の zoom-pon、`lock_mode=toggle`、`tools\test_zoompon_e2e.ps1`）:

| 設定 | 実際の押下時間 | 反応した回数 |
|---|---|---|
| 修正前 `a` / `KeyHoldMs=40` | 約 15ms | 12 回中 3 回 |
| 修正前 `a` / `KeyHoldMs=600` | 約 15ms | 12 回中 6 回 |
| 修正後 `a` / `KeyHoldMs=120` | 約 127ms | **30 回中 30 回** |
| 押しっぱなしを既定にしたあと `a` | マウスを離すまで | **20 回中 20 回** |

修正前は「拡大していない状態から」は 100% 成功し、「拡大中から」が 1/10 まで
落ちていました。拡大鏡の描画で相手の tick が伸びるぶん、隙間が広がるためです。
症状が「たまに失敗する」ように見えても、**状態で層別すると規則性が出る**ことがあります。

押しっぱなしにする以上、**取り残しの経路を全部塞ぐ**必要があります。
プレフィクスの離上・`chord_flush`・`reconcile`・60 秒の保険、いずれの経路でも
`hold_end()` を呼びます。押しっぱなしのまま mayous を終了させても離されることを
`tools\test_hold_release.ps1` で検証しています。

**離している時間にも下限が要る。** プレフィクスを押したまま同じ組み合わせを
もう一度成立させると、押し直しが必要になります。ここで「離して即座に押し直す」と、
離上と押下が同じ `chord_pump()` で連続して出るため隙間が **1ms 未満**になり、
ポーリング方式のアプリからは離した瞬間が一切見えません。つまり 2 回目以降が
無かったことになります。実測（`tools\test_refire.ps1`、1ms 間隔で観測）:

| | 左クリック 4 回に対して観測された押下 |
|---|---|
| 即座に押し直す | **3 回**（`1654 / 808 / 1180` ms — くっついている） |
| `KeyHoldMs` 空けて押し直す | **4 回**（`821 / 702 / 696 / 1100` ms） |

押している時間に下限が要るのと同じ理由です。`hold_begin()` は
`repress` フラグと `TIMER_KEYREL_BASE + pfx` で間を空けます。
間を空けている最中にプレフィクスが離された場合は、約束したぶんを
普通のタップとして出します（捨てると利用者のクリックが 1 回まるごと消える）。

**ただし、間が要るのは同じキーを押し直すときだけ。** 押しているキーと
これから押すキーが違うなら、「前のキーが離れ、別のキーが押された」ことは
ポーリング方式のアプリからも読めます。待つ理由が無いので、離上と押下を
同じ `chord_pump()` で続けて出してしまいます（`step_same()` で判定）。

これが効くのは、右クリック＋ホイール上に `a`、下に `b` のように
**上下へ別々のキーを割り当てた場合**です。回すたびに 1 回分の間
（`KeyHoldMs` = 既定 120ms）待たされていたのが、そのまま消えます。
実測（`tools\test_wheel_latency.ps1`、ホイールを送ってから
その押下がキーボードフックに見えるまでの時間）:

| | 1 回目 | 2 回目以降 |
|---|---|---|
| 修正前・上下交互（`a`/`b`） | 約 9ms | **約 127ms** |
| 修正後・上下交互（`a`/`b`） | 約 9ms | **約 10ms** |
| 上だけ連続（`a` の押し直し） | 約 6ms | 約 128ms（設計どおり） |

**同じキーの押し直しは、間隔を選べるようにした。** ここは押している時間
（`KeyHoldMs`）とは要求が別なので、設定を分けて `RepressGapMs` にした。
設定画面の「同じキーの押し直し」で **120 / 80 / 40 / 20ms** の 4 段階から選ぶ
（`config.c` の `kRepressGapMs[]`。ini に段階以外の値が書かれていたら
`cfg_repress_gap_snap()` が一番近い段階へ丸める）。実測:

| `RepressGapMs` | 2 回目以降の遅れ |
|---|---|
| 120（既定） | 約 127ms |
| 80 | 約 96ms |
| 40 | 約 49ms |
| 20 | 約 34ms |

**20ms を選んでも 34ms かかる。** 待ちは `SetTimer` で測っており、その分解能が
約 15.6ms あるためで、ここが実質の下限になる。これ以上詰めるには待ち方自体を
変えるしかない。

短くすると、242f6cb で直した「左クリック 4 回に対して押下 3 回」に近づく。
どこまで詰められるかは相手のアプリ次第なので、既定は 120ms のままにして
選択制にした。`tools	est_repress_gap.ps1` がラジオボタンから ini まで
4 段階すべて書けることを確かめている。

> **相手側の直し方も書いておく。** `GetAsyncKeyState` の戻り値は
> `0x8000`（いま押されているか）だけでなく `0x0001`（**前回この関数を
> 呼んでから押されたか**）を持っています。後者を見れば、周期の隙間に
> 収まった押下も拾えます。実測（`tools\test_asyncbit0.py`、周期 100ms に
> 対して押下 20ms）: `0x8000` は 8 回中 **2 回**しか拾えず、`0x0001` は
> 8 回中 **8 回**拾いました。ポーリングで待ち受ける側を書くときは
> `0x0001` も見ること。

### 7. 子ウィンドウは「先に作ったものほど上」

設定画面で、Alt+Tab のウィンドウ一覧が重なって離れた直後に、グループ枠の中の
コントロールがまとめて消える、という報告があった。`IsWindowVisible` で数えると
53 個すべて可視のままだったので、**消えたのではなく塗り潰されていた**。

原因は Z 順と `WS_CLIPSIBLINGS` の組み合わせ。`EnumChildWindows` は Z 順に上から
返すが、その並びは**作成順そのもの**だった ── つまり子ウィンドウは
**先に作ったものほど上**になる。タブ枠もグループ枠も中身より先に作るので、
放っておくと中身の上に乗る。

- `WS_CLIPSIBLINGS` **無し**（元の状態）… 中身は上の枠を無視して描けるので普通は
  見える。しかし枠が所有者描画で自分の矩形を `FillRect` すると、中身ごと
  塗り潰す。中身の更新領域は既に検証済みなので描き直されず、消えたままになる。
- `WS_CLIPSIBLINGS` **有り**だけ付ける … 今度は「上に乗った枠」に切り取られて
  中身が最初から一切描かれない（実際にこれで全部消えた）。

正解は**両方**。`sink_containers()` で枠とタブを `HWND_BOTTOM` へ沈めてから
`WS_CLIPSIBLINGS` を付ける。枠が下・中身が上、かつ互いに領域を侵さない状態になる。

> Z 順を直さずに `WS_CLIPCHILDREN` を親へ足しても解決しない。あれは
> 「親が子の領域を塗らない」だけで、兄弟同士の重なりには何の関係もない。

再現と確認は `tools\dbg_settings_paint.ps1`。子ウィンドウの可視状態を前後で比べ、
実画面から切り出す（`PrintWindow` は強制的に描き直させてしまうので使えない）。

### 8. 離上を他のツールに食べられたら、Raw Input で気づく

利用者からの報告: X-Mouse Button Control と MouseGestureL.ahk を併用中、
サイドボタンを押したあと**左クリックが効かなくなり、mayous を落としたら直った**。

低レベルフックは**後から設置したものほど先に呼ばれる**。同種のツールが手前に
居ると、mayous に届く前にイベントが消えることがある。プレフィクスの離上を
食べられると、mayous は保留(`PS_PENDING`)のまま居座り、以後の左クリックを
同時押しの相方として飲み込み続ける。保険は 60 秒後なので、体感では
「ずっと壊れている」。

厄介なのは、`GetAsyncKeyState` では**押しているのか離上を落としたのかを
区別できない**こと。プレフィクスの押下は自分で握り潰しているので、そもそも
async には現れない（[2](#2-getasynckeystate-は握り潰した押下を見ていない) の裏返し）。

**Raw Input はフックの握り潰しに関係なく届く。** 実測（`tools\test_rawinput.ps1`）:

```
  3265ms RAW   RIGHT DOWN [extra=00000000]  <- 握り潰した押下。アプリには届いていない
  5281ms RAW   RIGHT UP   [extra=00000000]
  5281ms RAW   RIGHT DOWN [extra=4D594F55]  <- mayous が離上時に注入したクリック
  5312ms ASYNC RIGHT DOWN                   <- ASYNC は注入分しか見ていない
```

`RAWMOUSE.ulExtraInformation` に `SendInput` の `dwExtraInfo` がそのまま入るので、
`MAYOUS_TAG` で自分の注入を除ける。`hDevice` は当てにしない（テストも `SendInput`
で押下を作るため、実機かどうかの判定には使えない）。

そこで `RIDEV_INPUTSINK` で常時登録し、物理状態を `g_physDown[]` に持つ。
`reap_lost()` が「物理的には離れているのに保留のまま」を見つけたら畳む。
**保留していたクリックは捨てずに世に出す** ── 利用者の右クリックを失わないため。

- 呼ぶのは `chord_on_mouse()` の入口（ボタンが動くたび）と `chord_sanity()`（保険）。
  1 秒タイマーだけに任せると、その間ずっと左クリックを飲み込むことになる。
- フックと `WM_INPUT` はどちらが先に処理されるか決まっていない。押下を見た時点で
  `g_physDown[btn] = TRUE` を自分で確定させる。そうしないと raw が遅れたときに
  生まれたての保留を殺してしまう。
- 猶予は `RAW_LOST_MS` = 250ms。本物の離上は数 ms で届くので誤爆しない。

登録を出し入れすると「登録前に離された」を取りこぼすので、起動時に一度だけ
登録して持ち続ける。マウス移動でも `WM_INPUT` は飛ぶが、フックはどのみち移動を
全部受けているので増える仕事はたかが知れている。実測（`tools\test_cpu.ps1`）:

| | 待機 5 秒 | 移動 8000 回 |
|---|---|---|
| 導入前 | 15.6 ms | 203.1 ms |
| 導入後 | 0.0 ms | 187.5 ms |

再現は `tools\test_lost_up.ps1`。mayous の**あとに** `eater.exe` を起動して
フックの手前に立たせ、物理的な離上を 1 回だけ食べさせる。

| | 食べられたあとの左クリック |
|---|---|
| 導入前 | **0 / 3**（すべて飲み込まれた） |
| 導入後 | **3 / 3**（保留していた右クリックもアプリに届いた） |

> **併用ツールは本当に居る。** この開発機では LG HUB・MouseGestureL・
> X-Mouse Button Control が常駐しており、`tools\dbg_x1_control.ps1` で
> **mayous を止めた状態でも**サイドボタン1 が「中クリック」としてアプリに
> 届くことを確認した。サイドボタン周りの挙動を調べるときは、まず
> mayous 抜きの対照実験を取ること。mayous のせいだと思ったものが
> よそのリマップだったということが起こる。

### 9. ダークモードは非公開 API に頼るしかない

Win32 の古いコントロールにはダークモードの公式 API がありません。Windows 自身も
uxtheme.dll の**序数エクスポート**（名前が無く番号でしか呼べない関数）を使っています。
`theme.c` も同じ方法ですが、非公開である以上いつ消えてもおかしくないので、
取得できなければ黙ってライトのまま動くようにしてあります。

```
序数 104  RefreshImmersiveColorPolicyState()
序数 132  ShouldAppsUseDarkMode() -> BYTE
序数 133  AllowDarkModeForWindow(HWND, BYTE)
序数 135  1809: AllowDarkModeForApp(BYTE) / 1903+: SetPreferredAppMode(int)
```

戻り値が 1 バイトの `bool` なので、`BOOL`(int) で受けると上位バイトのゴミを拾います。
`BYTE` で受けること。

コントロールの描画は `SetWindowTheme()` でテーマ名を差し替えて任せます
（`"DarkMode_Explorer"`、エディットとコンボは `"DarkMode_CFD"`）。
テーマが効かないグループ枠とタブは所有者描画にしています。タブは「中身の面」の
背景も塗ってくれないので、サブクラス化して `WM_ERASEBKGND` を差し替えています。
タイトルバーだけは公式 API（`DwmSetWindowAttribute`）があります。

---

## レジストリを使わない実装

設定は `mayous.ini`（`GetPrivateProfile*` / `WritePrivateProfile*`、常に絶対パス指定）。
**置き場所は exe と同じフォルダに固定**で、書けない場所にあっても `%APPDATA%` へは
逃がしません。逃がすと「設定がどこにあるか分からない」「フォルダを消しても設定が残る」
という、ポータブル運用で一番困る状態になるためです。書けない場合は起動時に知らせます。

自動起動の登録は**行いません**。スタートアップに入れるかどうかは使う人が決めることで、
アプリ側から勝手に登録するのは余計な世話だという判断です。

レジストリに触れるのは `startup_cleanup_legacy()`（`legacy.c`）一箇所だけで、旧バージョンが
`HKCU\...\Run` に書いた値を**削除する方向にしか働きません**。値が無い環境では
読み取り専用で開いて存在を確認して終わりです。

ダーク判定も、まず uxtheme の `ShouldAppsUseDarkMode()` に聞き、それが取れないときだけ
`Personalize\AppsUseLightTheme` を**読むだけ**です。

`tools\test_no_registry.ps1` が、一通りの操作の前後で HKCU のスナップショットを
比較して変化がないことを検証します。

---

## ビルド

MinGW-w64（GCC）が必要です。

```powershell
winget install BrechtSanders.WinLibs.POSIX.UCRT
.\build.bat
```

`build\mayous.exe` ができます。

```
build.bat debug     デバッグビルド。%TEMP%\mayous_debug.log にトレースを吐く
```

アイコンは `tools\make_icons.ps1` が生成します（外部素材なし）。
`res\mayous.ico` が無ければ `build.bat` が自動的に呼びます。

### マニフェストの差し替え

MinGW は非共有リンク時に `default-manifest.o` を必ず巻き込むため、`.rc` に自前
マニフェストを入れると ld が `multiple non-default manifests` で落ちます。
`build.bat` は自前マニフェストを `default-manifest.o` という名前で作り、`-B` で
探索パスの先頭に置いて本家を差し替えています。

### 文字コード

- `.bat` は **ASCII のみ**。Shift-JIS の 2 バイト目に `\` や `|` が現れるため、
  コードページ 932 以外のコンソールで行が壊れます。日本語は `.md` 側に置きます。
- `.rc` も ASCII のみ（windres のコードページ解釈に振り回されないため）。
  UI の日本語文字列は C ソースの `L"..."` に置いています。
- `.ps1` は **UTF-8 BOM 付き**。BOM が無いと PowerShell 5.1 が ANSI として読み、
  日本語コメントで構文エラーになります。

---

## テスト

```powershell
powershell -ExecutionPolicy Bypass -File tools\stress.ps1
```

状態機械の総当たり検証。**本物のウィンドウ**（`target.exe`）を相手に 14 通りの
操作列を注入し、アプリ側で**押下と離上が釣り合っているか**を検証します。
釣り合いが崩れる＝ボタンが押されっぱなし＝全クリックが壊れる、という最悪の
壊れ方を直接検出できます。

| スクリプト | 内容 |
|---|---|
| `tools\stress.ps1` | 状態機械の総当たり（14 シナリオ・収支検証） |
| `tools\run_tests.ps1` | 基本動作（抹殺・合成・昇格・素通し） |
| `tools\test_side.ps1` | サイドボタンの同時押し・単独差し替え |
| `tools\test_capture.ps1` | キー記録と保存 |
| `tools\test_settings.ps1` | 設定の保存と即時反映 |
| `tools\test_ui_behavior.ps1` | 二重起動時の挙動と [適用] の活性制御 |
| `tools\test_no_registry.ps1` | レジストリを汚さないことの検証 |
| `tools\test_left_latency.ps1` | 左クリック単押しの遅延（v13 以降は常に 0） |
| `tools\test_hold_release.ps1` | `hold:` のキーが必ず離されること |
| `tools\test_keydur.ps1` | 注入したキーが実際に何 ms 押されているかの実測 |
| `tools\test_autorepeat.ps1` | 注入した押下がオートリピートしないことの確認 |
| `tools\dbg_settings_paint.ps1` | 窓が重なって離れたあと設定画面が描き直されるか |
| `tools\test_side_none.ps1` | 「右クリック + サイドボタン = なし」周辺の収支 |
| `tools\test_lost_up.ps1` | 他ツールに離上を食べられても復帰できるか |
| `tools\test_rawinput.ps1` | 握り潰した押下が Raw Input には見えることの確認 |
| `tools\test_cpu.ps1` | マウス移動を流したときの CPU 時間 |
| `tools\dbg_x1_control.ps1` | mayous 抜きでサイドボタンがどう届くかの対照実験 |
| `tools\test_refire.ps1` | プレフィクスを押したまま何度も成立させたときの押し直し |
| `tools\test_zoompon_refire.ps1` | 同上を実物の zoom-pon で数える(要 zoom-pon) |
| `tools\test_asyncbit0.py` | GetAsyncKeyState の 0x0001 で短い押下を拾えるか |
| `tools\test_zoompon_e2e.ps1` | 実物の zoom-pon を相手にした反応率（要 zoom-pon） |
| `tools\test_wheel_latency.ps1` | 同時押しのキーがホイールから何 ms 遅れて出るか |
| `tools\test_repress_gap.ps1` | 「同じキーの押し直し」のラジオが ini へ書けるか |
| `tools\test_regkey.ps1` | 登録キーの実測（素通し・握り潰し・遅延・押しっぱなし）と設定画面 |
| `tools\shot_settings.ps1` | 設定画面のキャプチャ（目視確認用） |

### テストを書くときの落とし穴

> **ポインタを引数に取るウィンドウメッセージを、プロセスをまたいで `SendMessage`
> してはいけません。** 渡したポインタは呼び出し側のアドレス空間のものなので、
> 相手プロセスがそこへ書き込もうとしてアクセス違反で落ちます。
> `TCM_GETITEMRECT` でこれをやってしまい、「タブをクリックすると落ちる」という
> **実在しないバグ**を延々と追いかけました。相手プロセス内に `VirtualAllocEx` で
> 領域を確保し、そのアドレスを渡してから `ReadProcessMemory` で読み戻してください
> （`tools\uilib.ps1` の `RemoteRectMessage`）。

> `probe.exe` はフック鎖の中に居るため、OS がフックを呼ぶ順番次第で上流にも
> 下流にもなり得ます（実測で入れ替わることを確認）。並び順に依存しない計測が
> 必要な場面では `target.exe` を使ってください。

> 別プロセスのウィンドウに `SetForegroundWindow` しても効かないことがあり、
> その場合**最初のクリックはウィンドウのアクティブ化に消費されます**。
> 状態が変わるまで押し直す作りにしてください。

> **計測用の PowerShell は必ず DPI 対応にしてください。** DPI 非対応のままだと
> `GetWindowRect` が仮想化された座標（125% 環境なら実寸の 0.8 倍）を返し、
> 画面キャプチャのビットマップが小さすぎて右下が欠けます。「レイアウトがはみ出して
> いる」という実在しない不具合を追いかける羽目になります。`tools\uilib.ps1` は
> 読み込み時に `SetProcessDpiAwarenessContext` を呼んでいます。

> `PrintWindow`(PW_RENDERFULLCONTENT) は、ウィンドウによっては下半分が描かれない
> ことがあります（実際にライトテーマで起きました）。おかしいと思ったら
> `tools\shot_screen.ps1`（実画面からの切り出し）と見比べてください。

---

## リリース

**exe が変わったら必ず版数を 1 つ上げます。** 番号を手で入れると必ずどこかがずれるので、
`release.ps1` が現在の版数を `res\mayous.rc` から読み取って自動で繰り上げます。

```powershell
.\release.ps1              # 今の版数 +1 で出す(通常はこちら)
.\release.ps1 -Version 7   # 版数を明示する
.\release.ps1 -DryRun      # 何番になるかだけ見る
```

1. `res\mayous.rc` と `src\common.h` のバージョンを揃える
2. `build.bat` でビルド（常駐中の mayous があれば `--exit` で止めてから）
3. `dist\mayous-vN.zip`（`mayous.exe` + 利用者向け `README.txt`）を作る
4. `dist\mayous-vN.zip.sha256` を書き出す

`dist\` は毎回空にしてから作り直すので、置かれるのは最新の 1 つだけです。
過去の版は git の履歴に残ります。

### 作業の流れ

修正が終わったら、そのままコミットして push します。

```powershell
.\release.ps1        # exe が変わったときだけ。ドキュメントだけの修正では不要
git add -A
git commit -m "..."
git push origin master
```

tag は `release.ps1` が作りません（意図して付け替えることがあるため）。
必要なら手で付けてください。

```powershell
git tag -a v3 -m "Mayous v3"
git push origin v3
```
